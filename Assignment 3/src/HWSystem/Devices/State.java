package HWSystem.Devices;

/**
 * Represents the state of a device.
 * <p>
 * This enum defines two possible states for a device: {@code ON} and {@code OFF}.
 * </p>
 */
public enum State {

    /** Represents the device being ON. */
    ON,

    /** Represents the device being OFF. */
    OFF;
}
